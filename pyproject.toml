[build-system]
requires = ["setuptools>=77.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "metacellspy"
version = "0.1.0"
description = "Metacell analysis"
authors = [
    { name = "Oren Ben-Kiki", email = "oren@ben-kiki.org" },
]
readme = "README.rst"
license = "MIT"
license-files = ["LICENSE.rst"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
]
requires-python = ">= 3.10"
dependencies = [
    "dafpy",
    "juliacall",
    "numpy",
    "pandas",
]

[tool.setuptool]
data_files = ["juliapkg.json"]

[project.optional-dependencies]
dev = [
    "black",
    "bumpversion",
    "flake8",
    "isort",
    "jedi",
    "mypy",
    "mypy_extensions",
    "pylint",
    "pytest",
    "pytest-cov",
    "sphinx",
    "sphinx_rtd_theme",
    "twine",
]
test = [
    "jedi",
    "pytest",
    "pytest-cov",
]

[project.urls]
"Homepage" = "https://github.com/tanaylab/metacellspy"
"Bug Tracker" = "https://github.com/tanaylab/metacellspy/issues"

[tool.setuptools.packages.find]
exclude = ["htmlcov"]
